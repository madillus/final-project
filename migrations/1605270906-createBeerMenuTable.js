exports.up = async function (sql) {
  await sql`
  CREATE TABLE beer_menu (
    id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		name VARCHAR(255),
		german_name VARCHAR(255),
		description TEXT,
		german_description TEXT,
		allergens VARCHAR(10),
		price_taster decimal(12,2),
		price_small decimal(12,2),
		price_normal decimal(12,2),
		price_pitcher decimal(12,2)
    );

  `;
};

exports.down = async function (sql) {
  await sql`
    DROP TABLE IF EXISTS beer_menu;
  `;
};